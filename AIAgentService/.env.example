# Example environment configuration for AIAgentService

# Optional API key. If set, requests must include:
#   Authorization: Bearer <AI_API_KEY>
AI_API_KEY=change-me-or-leave-empty-for-dev

# Optional port variable (informational). Uvicorn command controls the actual port unless integrated.
AI_PORT=3010
